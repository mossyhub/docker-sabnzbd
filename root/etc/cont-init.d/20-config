#!/usr/bin/with-contenv bash

# make folders
mkdir -p \
	/downloads \
	/incomplete-downloads

#copy config
cp -rv /mnt/storage1/sabconfig/* /config

# permissions
chown abc:abc \
	/downloads \
	/incomplete-downloads \
	/config
